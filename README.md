# agent-linter
A real linter for AI agents.  `agent-linter` is a deterministic static analysis tool that checks executable agent behavior before it runs. It focuses on the agent execution surface: action definitions, action invocations, and execution wiring.
