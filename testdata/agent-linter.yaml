version: 1

rules:
  AL001:
    enabled: true
    severity: error

inputs:
  definitions:
    - "actions.json"
  invocations:
    - "invocations.jsonl"
  wiring:
    - "wiring.yaml"

environments:
  default: dev
