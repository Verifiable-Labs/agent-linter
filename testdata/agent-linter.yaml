version: 1

rules:
  AL001:
    enabled: true
    severity: error

inputs:
  definitions:
    - "testdata/actions.json"
  invocations:
    - "testdata/invocations.jsonl"
  wiring:
    - "testdata/wiring.yaml"

environments:
  default: dev
